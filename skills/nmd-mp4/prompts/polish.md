# 精校提示词（S4：原始转写稿 → 精校稿）

把语音转写稿精校为可出版的 Markdown 文稿。

输入：{{输入文件路径}}　输出：{{输出文件路径}}

## 必须做
- 修正同音/误识别错别字。术语对照表：{{术语表}}
- 按内容逻辑分段，加 ## 小标题
- 输出结构：`# 标题` → `> 概要`（3-5 行）→ 正文小节 → `## 要点总结`（5-10 条）

## 禁止
- 禁止改写观点、压缩内容、删减案例和故事（信息零丢失）
- 禁止改变口播风格（语气词、口头禅保留）

完成后回复一行：已完成 {{标题}}（字数）。
